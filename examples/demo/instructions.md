Build a demo application in Ruby that shows how to implement a bubble sort. A naive algorithm is okay.

Assume input is an array defined in code, like: `number = [17, 1, 42, 9, -2].freeze`.
The sorted list should equal: `[-2, 1, 9, 17, 42]`.