# Goal
The overall goal is to: ${instructions}

# Environment
The current code is:

``` ruby
${code}
```

# Task
- The code returns a syntax error. Rewrite the code to fix the error
- Keep all code comments

The error message is:

```
${error_message}
```

# Response
Respond in json using this format:

``` json
{
  "code": "New code"
}
```